

const Products = ({ products }) => {
  return (
    <div className="bg-gray-">
      {/* {products.map((product) => (
        <h1>{product.title}</h1>
      ))} */}
    </div>
  )
}

export default Products